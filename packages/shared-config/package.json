{
  "name": "@coachmeld/shared-config",
  "version": "1.0.0",
  "description": "Shared configuration and constants for CoachMeld applications",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@coachmeld/shared-types": "workspace:*"
  }
}